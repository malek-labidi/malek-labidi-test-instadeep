import express from 'express';
import { } from '../controllers/part1.js';

const router = express.Router();

// Handling routes for the '/tests' endpoint
router.route('/:str')
.post('/part1',(req, res)=>{
    
});

export default router;